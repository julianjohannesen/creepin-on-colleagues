extends layout

block content
    div
        p This is where the profile data should show up.
        p(class="name") #{name} has #{points} total points and #{badges} badges.
        //- Add a recent searches feature using cookies
