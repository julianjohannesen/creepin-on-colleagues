extends layout
block content
    h1= title
    p Welcome to #{title}
    form(action = "/", method = "POST")
        label(for = "username") Username:
        input(name = "username" type = "text" value = "")
        button(type = "submit") Creep